// In your main products file (products/main.js)
import {
	initializeProducts,
	loadPage,
	nextPage,
	previousPage,
} from "./productUI.js";

// The system will automatically initialize when DOM loads
