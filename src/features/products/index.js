import {
	initializeProducts,
	loadPage,
	nextPage,
	previousPage,
	getPaginationState,
} from "./productUI.js";

document.addEventListener("DOMContentLoaded", () => {
	initializeProducts();
});
